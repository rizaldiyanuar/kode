<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Note extends Model
{
    // protected $tabel = 'ngasal_id'; (jika nama tabel dan class tidak sama)
    // protected $primaryKey = 'ngasal.id'; (jika primariKey bukan id)
    // public $timestamps = false; (disable created_at dan update_at)
}
